package com.degree.homedash

/**
 * App-wide build identity. Shown in the Home header so it's obvious which build is running.
 *
 * Keep [VERSION] in step with `versionName` in composeApp/build.gradle.kts.
 */
object AppInfo {
    const val VERSION = "1.0"
}
