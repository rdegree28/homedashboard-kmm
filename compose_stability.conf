// Shared (:shared module) types that cross into @Composable parameters. Listing them here tells the
// Compose compiler to treat them as stable, so composables receiving them stay skippable.
com.degree.homedash.shared.model.HistoryPoint
com.degree.homedash.shared.network.ConnectionStatus
com.degree.homedash.shared.model.EntityState
